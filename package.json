{
  "name": "request-animation-frame-debouncer",
  "version": "0.0.2",
  "types": "./dist/raf-debounce.d.ts",
  "main": "./dist/raf-debounce.min.js",
  "scripts": {
    "build": "rm -rf ./dist && tsc -p ./ && babel ./dist/raf-debounce.js -o ./dist/raf-debounce.min.js",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-es2015-modules-umd": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-minify": "^0.3.0"
  }
}
